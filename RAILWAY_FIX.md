# 🔧 Исправление ошибки сборки API на Railway

## Проблема
```
error MSB3202: The project file "/src/LibraryMPT.Api/LibraryMPT.Api.csproj" was not found.
```

## Решение

### Вариант 1: Использовать Dockerfile.api из корня (рекомендуется)

1. В Railway для **LibraryMPT.Api** сервиса:
   - **Settings** → **Build & Deploy**
   - **Root Directory**: `/` (корень)
   - **Dockerfile Path**: `Dockerfile.api`
   - Сохрани изменения

2. Перезапусти деплой

### Вариант 2: Исправить LibraryMPT.Api/Dockerfile

Если Railway использует `LibraryMPT.Api/Dockerfile`, убедись что:
- **Root Directory** = `/` (корень репозитория, не `LibraryMPT.Api`)
- Dockerfile правильно копирует файлы относительно корня

### Вариант 3: Использовать Nixpacks вместо Dockerfile

1. Удали или переименуй `Dockerfile.api` и `LibraryMPT.Api/Dockerfile`
2. Убедись, что `LibraryMPT.Api/nixpacks.toml` существует
3. Railway автоматически использует Nixpacks

## Проверка

После исправления:
1. Запушь изменения в GitHub
2. Railway автоматически перезапустит деплой
3. Проверь логи - должно быть `dotnet restore` успешно

## Если всё ещё не работает

Проверь в Railway:
- **Root Directory** должен быть `/` (корень)
- **Dockerfile Path** должен указывать на правильный файл
- Убедись, что файлы `LibraryMPT.sln`, `LibraryMPT.csproj`, `LibraryMPT.Api/LibraryMPT.Api.csproj` есть в репозитории

