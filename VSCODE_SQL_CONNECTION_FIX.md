# 🔧 Исправление: "A SQL editor must have focus"

## Ошибка

```
mssql: A SQL editor must have focus before executing this command
```

## Причина

Расширение SQL Server в VS Code требует, чтобы был открыт **SQL файл** (`.sql`) перед выполнением команд подключения или запросов.

## Решение

### Шаг 1: Создай или открой SQL файл

1. **Создай новый файл:**
   - Нажми **Ctrl+N** (новый файл)
   - Сохрани как `test.sql` (или любое имя с расширением `.sql`)
   - Или открой существующий SQL файл (например, `Library.sql`)

2. **Убедись, что файл активен:**
   - Файл должен быть открыт в редакторе
   - Курсор должен быть в редакторе (не в терминале или другом месте)

### Шаг 2: Подключись к SQL Server

**Теперь, когда SQL файл открыт:**

1. Нажми **Ctrl+Shift+P**
2. Введи: `SQL: Connect`
3. Выбери **"SQL: Connect"**
4. Заполни данные подключения

### Шаг 3: Выполни SQL запрос

После подключения:

1. Напиши SQL запрос в открытом файле, например:
   ```sql
   SELECT * FROM Users;
   ```

2. Выдели запрос (или поставь курсор на строку с запросом)

3. Нажми **Ctrl+Shift+E** (Execute Query)
   - Или правой кнопкой → **"Execute Query"**

## Альтернатива: SQLTools Extension

Если расширение `mssql` не работает, попробуй **SQLTools**:

1. **Установи расширения:**
   - **SQLTools** (by Matheus Teixeira)
   - **SQLTools SQL Server** (driver)

2. **Подключись:**
   - Нажми на иконку **SQLTools** в боковой панели (слева)
   - Нажми **"Add New Connection"**
   - Выбери **SQL Server**
   - Заполни данные

3. **SQLTools не требует открытый SQL файл** для подключения!

## Быстрая проверка:

✅ SQL файл открыт (`.sql` расширение)  
✅ Файл активен (курсор в редакторе)  
✅ Затем выполняешь команду `SQL: Connect`

## Если всё ещё не работает:

1. Перезапусти VS Code
2. Проверь, что расширение `mssql` установлено и включено
3. Попробуй расширение **SQLTools** вместо `mssql`
4. Или используй **локальный SQL Server** для разработки через SSMS

