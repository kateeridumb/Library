# 🚨 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Убрать dotnet из SQL Server

## Проблема
Railway использует `railway.json` с командой `dotnet LibraryMPT.dll` для ВСЕХ сервисов, включая SQL Server.

## Решение (выполни ВСЕ шаги!)

### Шаг 1: В Railway UI для SQL Server сервиса

1. Открой сервис `sqlserver`
2. Перейди в **"Deploy"** (5-й пункт меню)
3. Найди поле **"Start Command"** или **"Command"**
4. **ОБЯЗАТЕЛЬНО УДАЛИ** всё содержимое (если там `dotnet LibraryMPT.dll` или что-то ещё)
5. Оставь поле **ПОЛНОСТЬЮ ПУСТЫМ**
6. **Сохрани изменения**

### Шаг 2: Запушь исправленный railway.json

Я убрал `startCommand` из `railway.json`, чтобы он не применялся к SQL Server.

```bash
git add railway.json
git commit -m "Remove startCommand from railway.json to fix SQL Server"
git push
```

### Шаг 3: Перезапусти деплой

1. В Railway нажми **"Redeploy"** для SQL Server сервиса
2. Проверь логи

### Шаг 4: Проверь результат

В логах должно быть:
```
=> [runtime] CMD ["/opt/mssql/bin/sqlservr"]
SQL Server is now ready for client connections
```

А НЕ:
```
The executable `dotnet` could not be found
```

## Если всё ещё не работает

### Вариант A: Удалить railway.json полностью

1. Удали `railway.json` из репозитория
2. Настрой всё через Railway UI:
   - Для MVC: в Deploy укажи `dotnet LibraryMPT.dll`
   - Для SQL Server: в Deploy оставь пустым

### Вариант B: Использовать разные конфиги

1. Переименуй `railway.json` → `railway.mvc.json`
2. Для SQL Server настрой всё через UI (без конфига)

## Важно!

✅ **Start Command в Railway UI для SQL Server ДОЛЖЕН БЫТЬ ПУСТЫМ**  
✅ **railway.json больше не содержит startCommand**  
✅ **Dockerfile.sqlserver правильный (без dotnet)**

