# 🗄️ Настройка SQL Server на Railway через Empty Service

## Шаг 1: Создание Empty Service

1. В проекте Railway нажми **"+ New"**
2. Выбери **"Empty Service"**
3. Назови его `sqlserver` (или любое другое имя)

## Шаг 2: Настройка Source (Источник)

1. Открой сервис `sqlserver`
2. Перейди в **"Source"** (первый пункт в меню слева)
3. Нажми **"Connect GitHub"** (если ещё не подключен)
4. Выбери свой репозиторий `LibraryMPT`
5. **Root Directory**: оставь пустым `/` (корень) или укажи `/` явно

## Шаг 3: Настройка Build (Сборка)

1. Перейди в **"Build"** (4-й пункт в меню)
2. Ты увидишь три секции:
   - **Railpack** (по умолчанию) — не используй для SQL Server
   - **Metal Build Environment** — можно включить (переключатель)
   - **Custom Build Command** — не нужен для SQL Server

3. **Важно**: Railway должен автоматически обнаружить `Dockerfile.sqlserver` и использовать Docker вместо Railpack
   
4. Если Railway всё ещё использует Railpack:
   - В секции **Railpack** нажми на стрелку вниз (chevron)
   - Выбери **"Use Dockerfile"** или **"Docker"**
   - Или создай файл `.railwayignore` в корне проекта (Railway тогда будет использовать Dockerfile)

5. **Dockerfile Path**: `Dockerfile.sqlserver` (должен определиться автоматически)
6. **Docker Context**: оставь пустым (по умолчанию корень)

## Шаг 4: Настройка Deploy (Деплой)

1. Перейди в **"Deploy"** (5-й пункт)
2. **Start Command**: оставь пустым (SQL Server запустится автоматически)
3. **Healthcheck Path**: оставь пустым
4. **Restart Policy**: `ON_FAILURE` (перезапуск при ошибке)

## Шаг 5: Настройка Variables (Переменные окружения)

1. Перейди в **"Variables"** (вкладка вверху, рядом с "Deployments")
2. Нажми **"+ New Variable"**
3. Добавь переменные:

   **Переменная 1:**
   - **Name**: `SA_PASSWORD`
   - **Value**: `3rXz6LHjVJu6xNS` (твой пароль)
   - **Type**: `Secret` (рекомендуется для безопасности) или `Plain Text`

   **Переменная 2:**
   - **Name**: `MSSQL_PID`
   - **Value**: `Express`
   - **Type**: `Plain Text`

   **Переменная 3:**
   - **Name**: `ACCEPT_EULA`
   - **Value**: `Y`
   - **Type**: `Plain Text`

4. Сохрани все переменные

## Шаг 6: Настройка Networking (Сеть)

1. Перейди в **"Networking"** (2-й пункт в меню)
2. Нажми **"Generate Domain"** (если ещё не создан)
3. Запиши полученный домен, например: `sqlserver-production.up.railway.app`
4. **Port**: должен быть `1433` (стандартный порт SQL Server)

## Шаг 7: Запуск и проверка

1. Вернись в **"Deploy"**
2. Railway автоматически начнёт сборку и деплой
3. Дождись зелёного статуса (✅ Deployed)
4. Проверь логи (вкладка **"Deployments"** → выбери последний деплой → **"View Logs"**)

## Шаг 8: Получение Connection String

После успешного деплоя:

1. Перейди в **"Networking"**
2. Скопируй **Public Domain** (например: `sqlserver-production.up.railway.app`)
3. Connection String будет:
   ```
   Server=sqlserver-production.up.railway.app,1433;Database=ElectronicLibraryv5;User Id=sa;Password=3rXz6LHjVJu6xNS;TrustServerCertificate=True;Encrypt=True;
   ```
   (замени `sqlserver-production.up.railway.app` на свой Public Domain из Railway)

## Шаг 9: Подключение через SSMS

1. Открой **SQL Server Management Studio**
2. **Server name**: `sqlserver-production.up.railway.app,1433`
3. **Authentication**: SQL Server Authentication
   - **Login**: `sa`
   - **Password**: `3rXz6LHjVJu6xNS`
4. Нажми **Connect**

## ⚠️ Важные замечания

- **SA_PASSWORD** должен быть сложным (минимум 8 символов, буквы, цифры, спецсимволы)
- Не коммить пароли в Git — используй только Variables в Railway
- После первого подключения создай базу данных через SSMS
- Если не подключается — проверь логи в Railway и убедись, что сервис запущен (зелёный статус)

## 🔧 Troubleshooting

**Ошибка "Port already in use":**
- В Railway порт `1433` должен быть автоматически настроен
- Если проблема — попробуй перезапустить сервис

**Ошибка "Login failed":**
- Проверь правильность пароля `SA_PASSWORD` в Variables
- Убедись, что используешь `sa` как username

**SQL Server не запускается:**
- Проверь логи в Railway (Deployments → View Logs)
- Убедись, что все переменные (`SA_PASSWORD`, `MSSQL_PID`, `ACCEPT_EULA`) установлены

